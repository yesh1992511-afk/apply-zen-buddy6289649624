// Shared helpers loaded inline by content scripts via <script>-less injection:
// each content script defines its own copy of jitter() and sendJobs() locally
// because MV3 doesn't let content scripts share modules without bundling.
// This file exists for human readers only and is intentionally not loaded.
